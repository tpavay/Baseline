import Testing
@testable import Baseline

struct BaselineTests {
    @Test func appModuleLinks() {
        // Sanity check that the test target links against the app module.
        // Replace with real tests as pure logic (HRV/readiness math) lands.
        #expect(Bool(true))
    }
}
