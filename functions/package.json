{
  "name": "baseline-functions",
  "description": "Baseline Conversation Runtime \u2014 provider-agnostic LLM proxy for the Context Engine.",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/tools.test.js test/prompt.test.js test/workoutImport.test.js test/workoutImportRuntime.test.js test/llmObservability.test.js test/workoutImportStream.test.js",
    "test:firestore-emulator": "npm run build && firebase --config ../firebase.json emulators:exec --project baseline-import-test --only firestore \"node --test test/workoutImportFirestore.integration.test.js\"",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@langfuse/otel": "^5.9.1",
    "@langfuse/tracing": "^5.9.1",
    "@opentelemetry/sdk-node": "^0.220.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}